import { BlockToolData } from '@editorjs/editorjs';

/**
 * VAR_TOOL_NAME Tool's input and output data format
 */
export interface VAR_TOOL_NAMEData extends BlockToolData {}

/**
 * VAR_TOOL_NAME Tool's configuration object that passed through the initial Editor config
 */
export interface VAR_TOOL_NAMEConfig {}